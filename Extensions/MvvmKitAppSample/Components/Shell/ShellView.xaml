<UserControl x:Class="MvvmKitAppSample.Components.Shell.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:t="clr-namespace:MvvmKit;assembly=MvvmKit"
             xmlns:root="clr-namespace:MvvmKitAppSample"
             xmlns:local="clr-namespace:MvvmKitAppSample.Components.Shell"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{StaticResource dt.Shell.ShellVm}"
             >
    <UserControl.Resources>
        <t:SelectiveResources>
            <ResourceDictionary.MergedDictionaries>
                <t:DesignTimeResource Source="DtShell.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </t:SelectiveResources>
    </UserControl.Resources>
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Title}"/>
        <Button DockPanel.Dock="Top" Margin="10" Padding="10" Command="{Binding ChangeACommand}">Change Property</Button>
        <ContentControl DockPanel.Dock="Top" Height="100" t:RegionHost.Region="{x:Static root:GlobalNav.Main}"></ContentControl>
        <ContentControl DockPanel.Dock="Top" t:RegionHost.Region="{x:Static root:GlobalNav.Gain}"></ContentControl>
    </DockPanel>
</UserControl>
